<div class="span12">
    <div class="who">
        <%= link_to("Edit", edit_who_path(@who) , :class => "btn btn-mini btn-primarypull-right") %>
    </div>

    <h2> <%= @who.title %> </h2>

    <p> <%= @who.description %> </p>

</div>
